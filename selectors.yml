selectors: 
  - name: dim_except_listings_w_hosts
    description: Select all dim models, executing dim_listings_w_hosts.
    definition: 
      union:
        - method: path        # its like path:model/dim
          value: models/dim
        - exclude:
            - method: fqn
              value: dim_listings_w_hosts