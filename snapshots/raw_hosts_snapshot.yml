snapshots:
  - name: scd_raw_hosts
    relation: source('airbnb', 'hosts')
    config:
      unique_key: id
      updated_at: updated_at
      strategy: timestamp
      # what if delete the record
      hard_deletes: invalidate